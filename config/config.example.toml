# Nido Configuration
# Location: ~/.config/nido/config.toml

[assistant]
name = "Nido"
language = "fa"

[hotkey]
# Push-to-talk key. Default is F9.
key = "KEY_F9"
# Optional evdev device path, e.g. "/dev/input/event3". Empty string enables auto-discovery.
device = ""

[audio]
# Optional sounddevice device name or index. Empty string uses default input device.
device = ""
sample_rate = 16000
channels = 1
max_seconds = 30

[ui]
# Position on screen: "top-right", "top-left", "bottom-right", "bottom-left"
position = "top-right"
# Auto-hide timeout in milliseconds after completion
timeout_ms = 4000
# Compact UI mode
compact = false

[stt]
# Local directory for Shenava Koochik sherpa-onnx model
model_dir = "~/.local/share/nido/models/shenava"
threads = 4

[laya]
# Local directory for Laya multilingual MLX model
enabled = true
model_dir = "~/.local/share/nido/models/laya-multilingual-mlx"
dtype = "float16"
device = "auto"
batch_size = 16
compile = true
cache_prompts = true
pad_to_multiple = 16
max_elements = 120
max_candidates = 24
shortlist_size = 20

[desktop]
enabled = true
max_steps = 24
max_elements = 120
max_depth = 32
settle_delay_ms = 120
action_timeout_ms = 2000
snapshot_timeout_ms = 1500
include_invisible = false
include_offscreen = false
accessibility_backend = "auto"
input_backend = "auto"

[apps]
# Whitelist and aliases for open_app tool
browser = "firefox"
chrome = "google-chrome"
terminal = "konsole"
editor = "code"
file_manager = "dolphin"
files = "dolphin"
calculator = "kcalc"
settings = "systemsettings"
music = "elisa"
telegram = "telegram-desktop"
discord = "discord"

[system]
auto_start = true
# Dangerous commands disabled by default
allow_shutdown = false
allow_reboot = false
